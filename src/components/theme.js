// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Inter, sans-serif", // Replace 'YourCustomFont' with your desired font family
  },
  palette: {
    mode: "light", // Set the default mode
  },
});
export default theme;
